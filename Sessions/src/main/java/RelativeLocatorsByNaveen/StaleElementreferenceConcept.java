package RelativeLocatorsByNaveen;

import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.interactions.Actions;

import io.github.bonigarcia.wdm.WebDriverManager;

public class StaleElementreferenceConcept {
	
	public static void main(String[] args) throws InterruptedException {
		
		
WebDriverManager.chromedriver().setup();
		
		ChromeOptions co = new ChromeOptions();
		
		co.addArguments("--remote-allow-origins=*");
		WebDriver driver = new ChromeDriver(co);
		
		driver.get("https://www.freshworks.com/");
		Thread.sleep(2000);
		driver.findElement(By.id("onetrust-button-group")).click();

		JavascriptExecutor js = (JavascriptExecutor)driver;
		
List<WebElement> eleList =driver.findElements(By.xpath("//footer[@class='sc-6293d692-0 dBdSZt']//li/a"));
//System.out.println(eleList.size());

for(int i =0;i<eleList.size();i++)
{
	System.out.println(eleList.get(i).getText());
                 
		 js.executeScript("arguments[0].click();",eleList.get(i));
		 //eleList.get(i).click();
	
		eleList =driver.findElements(By.xpath("//footer[@class='sc-6293d692-0 jBqqJa']//li/a"));
		System.out.println(eleList.size());
	}							
	}
	}





































































































































































































































































































































































































































































































































